{
  "name": "n8n-on-render",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted n8n on Render with safe auto-updates and production config",
  "engines": {
    "node": ">=20.15.0"
  },
  "scripts": {
    "build": "n8n build",
    "start": "n8n start",
    "healthcheck": "curl -f http://localhost:$PORT/healthz || exit 1"
  },
  "dependencies": {
    "n8n": "latest"
  }
}

