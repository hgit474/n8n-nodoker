{
  "name": "n8n-on-render",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted n8n on Render with safe auto-updates and production config",
  "engines": {
  "node": ">=18 <21"
  },
  "scripts": {
    "build": "n8n build",
    "start": "n8n start",
      "healthcheck": "node -e \"require('http').get('http://localhost:' + (process.env.PORT || 5678))\""
  },
  "dependencies": {
    "n8n": "latest"
  }
}
